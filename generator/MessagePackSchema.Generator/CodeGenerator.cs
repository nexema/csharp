namespace MessagePackSchema.Generator
{
    internal static class CodeGenerator
    {
        public static void GenerateFor(GenerateInput input)
        {
            foreach(var package in input.Packages)
            {
                foreach(var file in package.Files)
                {
                    
                }
            }
        }
    }
}